# VirtualStudent Sandbox 虚拟学生试验台 —— 技术设计文档

| 文档项 | 内容 |
|---|---|
| 文档名称 | 技术设计文档（Technical Design Document） |
| **版本** | **v5.0（唯一性升级：LLM 深度参与生成 + 数学唯一性保证）** |
| 日期 | 2026-07-24 |
| 关联文档 | 《需求说明文档-虚拟学生试验台-v3.0.md》（功能编号 FR-* 与验收标准以需求文档为准）；《虚拟学生、教师与家长全方位人格档案系统设计文档-v3.0.md》（**画像数据源与字段规范**） |
| 交付对象 | 参赛团队 + 下游 vibe coding 开发工具 |
| 文档约定 | 标注 TODO 的阈值/权重需试点后标定；**标注【v3.0 变更】的为本次版本修订点** |

---

## 0. 版本变更说明（v5.0）

### 0.1 变更驱动

v4.0 的 `PersonaService` 是"随机采样 + 规则派生 + LLM 润色"的伪随机流水线，无法保证唯一性（生日悖论下大规模必撞车）。用户明确要求：**LLM 必须深度参与生成，每个虚拟学生/教师/家长/机构独一无二，不能伪随机；规模以数千为主、偶尔上万**。v5.0 将 `PersonaService` 重构为**LLM 深度参与的四层管线 + 唯一性数学保证**。

### 0.2 变更清单总览（v4.0 → v5.0）

| # | 变更点 | v4.0 | v5.0 | 影响章节 |
|---|---|---|---|---|
| C-17 | **PersonaService 生成方式** | 随机采样 90% + LLM 润色 10% | **四层管线**：L1 随机骨架→L2 LLM 身份种子→L3 规则+LLM 派生→L4 指纹校验去重 | §5 M2 |
| C-18 | **唯一性机制** | 无 | **FingerprintEngine**：sha256 身份指纹 + 布隆过滤器去重 + 相似度检测 + 差异化改写 | §4.11 |
| C-19 | **LLM 调用模式** | 单次渲染 | **分层调用 + 批处理 + 温度分层**（种子层高多样/叙事层保一致），成本可控 | §8.7 |
| C-20 | **全角色唯一性** | 仅学生 | 教师（教学风格签名）/家长（教养风格签名）/机构（I1–I2 机构画像）全部 LLM 生成 | §5 M2 |
| C-21 | **规模与性能** | 未明确 | **分级设计**：20/5千/2万/20亿 的调用量/成本/时间/分布式方案 | §11 |

### 0.3 变更清单总览（v3.0 → v4.0，历史记录）

| # | 变更点 | v3.0 | v4.0 | 影响章节 |
|---|---|---|---|---|
| C-11 | **L-Model 引擎** | 单主体时间轴 | **L-Model 2.0**：多主体并行 + 社交网络交互 + 事件引擎 + 长周期 + 反事实 | §4.6, §5 M4 |
| C-12 | **社会网络模块** | 无 | **SocialNetworkEngine**：动态图（networkx），选择+影响效应传播 | §4.8, §5 M4 |
| C-13 | **事件引擎** | 定时事件 | **EventEngine**：定时+随机事件，半衰期衰减，状态+网络双改变 | §4.9 |
| C-14 | **数据库** | 5 表 | **+ `social_networks` / `relationships` / `life_events` / `counterfactual_runs` 4 表** | §6 |
| C-15 | **反事实框架** | 无 | **CounterfactualEngine**：同一起点+固定种子，变量分流，轨迹对比 | §4.10, §5 M4 |
| C-16 | **前端** | 10 页 | **+ 社会网络可视化 / 生命历程回放 / 反事实对比 3 页** | §10 |

### 0.3 变更清单总览（v2.0 → v3.0，历史记录）

| # | 变更点 | v2.0（旧） | v3.0（新） | 影响章节 |
|---|---|---|---|---|
| C-1 | **M2 模块重构** | 6 参数采样 + 认知引擎 | **画像服务（PersonaService）**：五阶段管线生成三类智能体（学生/教师/家长），含一致性引擎 | §5 M2 |
| C-2 | **L-Model 引擎** | 无（课时 step 隐含在 M4） | **独立 L-Model 模块**：分钟级连续时间轴、五场景事件流、疲劳/遗忘连续演化 | §4.6, §5 M4 |
| C-3 | **多智能体角色** | 8 角色（学生侧） | **+ 教师智能体（T-Model）+ 家长智能体（P-Model）**；干预多通道传递 | §3.1, §4.7 |
| C-4 | **数据库 schema** | `personas` 单表（6 参数 JSON） | **5 表结构**：`students` / `teachers` / `parents` / `classes` / `daily_timelines`（L-Model 事件流） | §6 |
| C-5 | **干预传递机制** | direct 单通道 | **5 通道**：direct / teacher_mediated / parent_mediated / shadow_edu_mediated / self_study_mediated | §5 M4, §8.5 |
| C-6 | **隐私中间件** | 原则性声明 | **PrivacyGuard 中间件**：P/R/S 三级强制，prompt/响应/导出三层过滤 | §3.5, §8.6 |
| C-7 | **prompt 注入** | 6 参数摘要 | **PersonaView 结构化注入**：P/R 级字段摘要（≤200 token），S 级永不进 | §8.2 |
| C-8 | **校准目标扩展** | 学生侧 4 指标 | **+ 教师/家长画像分布校准**（ Big Five/家庭结构/补习时长等 vs 常模） | §4.4, §5 M3 |
| C-9 | **DAG 节点扩展** | n1–n10 | **n3 拆分为 n3a（学生画像）/n3b（教师画像）/n3c（家长画像）**；n5 改为 L-Model 事件流模拟 | §7 |
| C-10 | **前端页面** | 6 页 | **+ 画像浏览页 / L-Model 时间轴页 / 场景比较页 / 子群切片面板** | §10 |

### 0.3 与档案设计文档的关系

```
┌─────────────────────────────────────────────────────────────┐
│  《虚拟学生、教师与家长全方位人格档案系统设计文档-v3.0》          │
│  = 画像数据规范 + 生成算法（数据源）                             │
│  提供：PersonaService.generate_students/teachers/parents()     │
│       五阶段管线 + 一致性引擎 + 14 张常模 YAML                   │
│  输出：students/teachers/parents 表的完整 JSON                  │
└──────────────────────┬──────────────────────────────────────┘
                       │ 调用（画像生成服务）
                       ▼
┌─────────────────────────────────────────────────────────────┐
│  本文档（技术设计文档-v3.0）                                    │
│  = 系统实现规格（消费方）                                       │
│  消费：simulation_vector / teacher_simulation_vector /         │
│       parent_simulation_vector（认知引擎唯一接口）               │
│  实现：L-Model 引擎、多通道干预传递、校准、差距量化、预筛         │
└─────────────────────────────────────────────────────────────┘
```

**接口契约**：档案系统暴露 `PersonaService` 三个生成函数与 `simulation_vector` 读取接口；本系统**不直接读取叙事档案字段**（如 name/family_story），只读 `simulation_vector` 与 P/R 级展示字段。这保证"改档案不动模拟，改模拟不动档案"。

---

## 1. 总体架构【v3.0 变更：新增画像服务与 L-Model 层】

> **价值导向（v5.1）**：本文档的每一项技术决策都服务于一个总纲——**超越真实，落地于真实**。CounterfactualEngine（反事实实验）、L-Model 2.0（7×24 长周期追踪）、SocialNetworkEngine（网络操纵）、PrivacyGuard（敏感议题安全研究）分别对应传统方法"物理上/伦理上/经济上做不到"的能力；Prefect 编排 + 批处理 LLM 调用 + DuckDB 轻量栈对应"效率与成本的数量级优势"。完整的价值论证（10 类不可能问题、四维价值、价值边界）见《需求说明文档》§1A。

系统分六层：接入层（React + FastAPI）、编排层（Prefect DAG）、**画像服务层（PersonaService：学生/教师/家长生成）**、智能体层（认知引擎 + 语言智能体 + T-Model + P-Model + 统计分析群）、**L-Model 引擎层（连续时间轴 + 五场景事件流）**、数据层（DuckDB 5 表 + 溯源图）、基座层（百炼 Qwen + AgentScope）。

![系统总体架构-v3.0](figures/fig1_architecture_v3.png)

**架构关键决策（v3.0 更新）**：

1. **画像服务独立成层**：PersonaService 是独立 Python 包（可单独测试/复用），暴露 `generate_students(n, config)` / `generate_teachers(n, config)` / `generate_parents(n, config)` 三个函数；内部实现档案文档的五阶段管线与一致性引擎。
2. **L-Model 独立成层**：生活时间引擎是独立的调度器，接收"画像 + 干预配置 + 场景规则"，输出"事件流 + 虚拟轨迹"；认知引擎退化为 L-Model 调用的"单场景学习增益计算器"。
3. **多智能体物理分离**：学生/教师/家长是三组独立 AgentScope 智能体实例，各自有 persona；干预传递函数（T-Model/P-Model）是确定性计算模块，不是 LLM 智能体。
4. **隐私中间件前置**：所有 LLM 调用、API 响应、文件导出必经 PrivacyGuard 过滤（P/R/S 三级）。

## 2. 技术选型总表【v3.0 不变】

（与 v2.0 §2 完全一致：Qwen-Plus/Max、DashScope、AgentScope、Prefect、pyBKT、Optuna、DuckDB、FastAPI、React、Python 3.11+。**无变更**。）

## 3. 多智能体系统设计【v3.0 重大变更】

### 3.1 角色与权责（扩展）

| 角色 | 类型 | 职责 | 禁止事项 |
|---|---|---|---|
| CognitiveEngine | 确定性 | BKT/ACT-R 单场景学习增益计算 | —— |
| LanguageAgent | LLM（千问） | 学生作答文本/错误解释生成 | 决定对错；引入画像外知识 |
| **TeacherAgent**【新增】 | 确定性 + LLM | **T-Model 传递函数**（保真度/风格匹配计算，确定性）；课堂讲解/反馈文本生成（LLM，可选） | 决定学生掌握度 |
| **ParentAgent**【新增】 | 确定性 + LLM | **P-Model 传递函数**（参与风格乘数，确定性）；家长辅导对话生成（LLM，可选） | 决定学生掌握度 |
| InterventionDriver | 确定性 | 干预配置解析与场景路由 | —— |
| **L-Model Engine**【新增】 | 确定性 | 连续时间轴调度、五场景事件流生成、疲劳/遗忘连续演化 | —— |
| OutputValidator | 确定性 | 输出契约校验 | —— |
| Calibrator | 统计 | 参数后验拟合 | 手调参数 |
| GapAnalyst | 统计 | 差距/失真/heldout | —— |
| PriorityRanker | 确定性 | 优先级评分 | 失真区给高优先级 |
| ReportWriter | LLM（千问） | 《科学假设与研究计划》撰写 | 编造数值与文献 |
| HITLReviewer | 人 | 否决/加权 | —— |

### 3.2 五条护栏（不变）+ 【v3.0 新增第六条】

（G1–G5 同 v2.0。）

**G6【新增】**：S 级隐私字段（D11/D17/家长教养方式等）**永不**进入任何 LLM prompt、API 响应、导出文件；由 PrivacyGuard 中间件代码级强制（§3.5）。

### 3.3 运行状态机【v3.0 扩展】

`initialized → data_loaded → kc_fitted → **personas_generated → teachers_generated → parents_generated** → calibrated → **l_modeled** → simulated → gap_analyzed → ranked → reported`，异常分支不变。

### 3.4 耦合机制（单次作答生命周期，不变）

（与 v2.0 §3.4 一致：认知引擎裁定 → LLM 生成 → 校验 → 写入。**v3.0 变化**：作答发生在 L-Model 事件流的某个场景事件中，而非固定 step。）

### 3.5 PrivacyGuard 隐私中间件【v3.0 新增】

```python
class PrivacyGuard:
    """P/R/S 三级可见性强制中间件"""
    LEVELS = {"P": ["identity", "personality_tags", "aptitudes", "character_practice", "interests"],
              "R": ["family", "academics", "health_public", "school_context", "after_school_public"],
              "S": ["private_sensitive", "gifted_sen", "parenting_style", "family_conflict", "tutoring_cost"]}

    def filter_for_prompt(self, persona: dict) -> dict:
        """只保留 P 级 + R 级摘要（≤200 token），S 级物理删除"""
    def filter_for_api(self, persona: dict) -> dict:
        """API 响应：P + R 级，S 级删除"""
    def filter_for_export(self, report: dict) -> dict:
        """导出物：P 级 + 聚合统计，S 级删除"""
    def scan_log(self, text: str) -> list[str]:
        """静态扫描：检测 S 级字段值是否泄漏到文本（T-P10）"""
```

所有 `call_qwen()`、FastAPI 响应、报告导出函数**强制**经过对应 filter；`scan_log` 用于验收测试 T-P10。

## 4. 核心算法与公式【v3.0 扩展】

### 4.1 BKT 掌握度更新（不变）

（同 v2.0 §4.1。）

### 4.2 ACT-R 遗忘【v3.0 变更：连续时间】

v2.0 按 step 离散近似；v3.0 在 L-Model 连续时间轴上演化：`A(t) = ln(Σ_j (t - t_j)^(−d))`，t 为当前时间戳（分钟），t_j 为历史练习时间戳。遗忘在每个事件边界连续更新。

### 4.3 虚拟效应量（不变）

（同 v2.0 §4.3：Hedges g + CI。）

### 4.4 校准距离与搜索【v3.0 扩展】

v2.0 四类目标（学习曲线/错误分布/首次正确/方差比）不变；**新增画像分布校准**：

| 拟合目标 | 距离度量 | 数据源 |
|---|---|---|
| 学生 Big Five 各维度均值/方差 | vs 青少年常模 | `personality_map.yaml` |
| 家庭结构比例（单亲/留守/孤儿） | vs 人口学分布 | `family_structure.yaml` |
| 影子教育时长分布 | vs CEPS/CFPS 量级 | `shadow_education.yaml` |
| 作息时间分布 | vs 公开统计 | `daily_schedule.yaml` |
| 教师经验/职称分布 | vs 教育统计 | `teacher_demographics.yaml` |

### 4.5 差距指标（不变）

（同 v2.0 §4.5。）

### 4.6 L-Model 2.0 多主体网络化连续时间引擎【v4.0 重构】

```python
class LifeTimeEngineV2:
    """多主体网络化生命历程模拟引擎（v4.0）"""
    def simulate(self, students: list[Student], teachers: list[Teacher],
                 parents: list[Parent], network: SocialNetwork,
                 interventions: list[Intervention], days: int,
                 events: EventSchedule) -> SimulationResult:
        """
        多主体并行 + 网络交互 + 事件驱动 + 长周期
        """
        for day in range(days):
            # 1. 每个主体推进自己的时间轴（v3.0 五场景）
            for student in students:
                timeline = self._simulate_day_single(student, ...)
            # 2. 社交场景交互（课间/午餐/社交媒体/约会）
            interactions = self._generate_social_interactions(students, network, day)
            # 3. 网络影响传播（选择+影响效应）
            network = SocialNetworkEngine().propagate(network, students, day)
            # 4. 事件触发（定时+随机）
            new_events = EventEngine().trigger(students, network, day, events)
            for event in new_events:
                self._apply_event_impact(event, students, network)
            # 5. 关系状态更新（恋爱/亲子/师生）
            self._update_relationships(students, network, day)
        return SimulationResult(trajectories, network_evolution, events_log)
```

**社交网络层（动态图）**：

```python
class SocialNetworkEngine:
    """同伴网络演化与影响传播"""
    def propagate(self, network: nx.Graph, students: list[Student], day: int) -> nx.Graph:
        # 选择效应：学业相似者更可能结成新边（同质性）
        # 影响效应：已有边两端学业状态趋同（社会影响）
        # 参考：[^190^][^186^][^187^]
    def influence_diffusion(self, network: nx.Graph, students: list[Student]) -> dict:
        # 学业状态沿边扩散：Δachievement_i = Σ_j (w_ij × (ach_j - ach_i) × susceptibility_i)
```

**事件引擎**：

```python
class EventEngine:
    """定时 + 随机事件，半衰期衰减"""
    def trigger(self, students, network, day, schedule) -> list[Event]:
        # 定时：考试/作息/学期节点
        # 随机：考砸/表白/获奖/家庭变故（按发生率抽样）
    def apply_impact(self, event: Event, students, network):
        # 改变个体状态（motivation/fatigue/stress）
        # 改变网络结构（新边/断边/孤立）
        # 改变关系状态（恋爱开始/分手/亲子冲突）
        # 影响按半衰期衰减：impact(t) = impact_0 × 0.5^(t / half_life)
```

### 4.7 多通道干预传递【v3.0，不变】

（同 v3.0 §4.7。）

### 4.8 反事实实验引擎【v4.0 新增】

```python
class CounterfactualEngine:
    """反事实轨迹分流"""
    def branch(self, baseline_state: SimulationState,
               modification: Callable[[SimulationState], SimulationState],
               days: int, seed: int) -> tuple[Trajectory, Trajectory]:
        """
        同一起点 + 固定种子，baseline 与 modified 分流
        返回：两条轨迹供对比
        用途："如果家长选择引导而非压制"、"如果禁止恋爱"、"如果换班"
        """
```

### 4.9 长周期追踪与回放【v4.0 新增】

`daily_timelines` 表支持跨天/跨学期连续存储；前端可按学生/时间范围查询轨迹，回放关键事件与状态曲线。

### 4.10 关系状态机【v4.0 新增】

```python
class RelationshipEngine:
    """恋爱/亲子/师生关系状态机"""
    # 恋爱：无→暗恋→朦胧→恋爱(轻/深)→分手，各状态转移概率与持续时间
    # 影响通道：时间投入挤占学业[^185^]、情绪波动、网络嵌入
    # 亲子/师生：互动质量随事件与应对方式动态变化
```

### 4.11 唯一性保证引擎【v5.0 新增核心】

```python
class FingerprintEngine:
    """身份指纹与唯一性保证（v5.0）"""
    KEY_FIELDS = ["name", "birth_date", "birth_place", "family_structure",
                  "parents_occupation", "parents_education", "key_life_events",
                  "aptitude_combo", "mbti", "core_personality"]

    def fingerprint(self, persona: dict) -> str:
        """身份指纹：关键字段归一化后的 sha256 哈希"""
        key_fields = {k: persona.get(k) for k in self.KEY_FIELDS}
        canonical = self._canonicalize(key_fields)
        return sha256(canonical.encode()).hexdigest()[:16]

    def similarity(self, persona_a: dict, persona_b: dict) -> float:
        """画像相似度（0-1），检测"近似重复"（撞脸）"""
        # 离散字段加权 Jaccard + 连续字段余弦相似度

class UniquenessGuarantor:
    """唯一性保证器：生成-校验-去重闭环"""
    def __init__(self):
        self.bloom = BloomFilter(capacity=20_000_000, error_rate=0.001)  # 分片布隆过滤器
        self.personas = []  # 已生成画像（用于相似度检测）

    def ensure_unique(self, candidate: dict, generator: Callable) -> dict:
        fp = FingerprintEngine().fingerprint(candidate)
        if fp in self.bloom:
            return self.ensure_unique(generator(), generator)  # 完全重复→重新生成
        # 近似重复检测（抽样比对，大规模用 LSH 局部敏感哈希加速）
        if self._max_similarity(candidate) > 0.85:
            return self._differentiate(candidate)  # LLM 改写差异化字段
        self.bloom.add(fp); self.personas.append(candidate)
        return candidate
```

**五场景增益分解**（认知引擎在每个场景事件中被调用）：

```python
def scene_gain(scene, student, item, context):
    base = bkt_update(student.p_know, item)
    if scene == "school":
        return base * teacher.fidelity * teacher.style_match
    elif scene == "shadow_edu":
        dose = student.shadow_education.weekly_hours
        return base * student.shadow_education.quality * dose_response(dose)
    elif scene == "parent":
        return base * parent.style_multiplier * parent.quality
    elif scene == "self_study":
        return base * student.self_regulation * student.strategy_repertoire
    elif scene == "fragment":
        return base * 0.3 * student.fragment_learning.efficiency
```

### 4.7 多通道干预传递【v3.0 新增】

```python
class InterventionDelivery:
    """干预按 target_scene 路由到对应传递函数"""
    def deliver(self, intervention: Intervention, scene: str,
                student: Student, teacher: Teacher, parent: Parent) -> float:
        if intervention.delivery_model == "direct":
            return 1.0  # 直连学生
        elif intervention.delivery_model == "teacher_mediated":
            return teacher.fidelity * teacher.style_match_for(intervention)
        elif intervention.delivery_model == "parent_mediated":
            return parent.style_multiplier_for(intervention)
        elif intervention.delivery_model == "shadow_edu_mediated":
            return student.shadow_education.quality * dose_response(...)
        elif intervention.delivery_model == "self_study_mediated":
            return student.self_regulation * student.strategy_repertoire
```

干预 YAML 新增字段：`target_scene: school/shadow_edu/home/self_study/all`、`delivery_model: direct/teacher_mediated/parent_mediated/shadow_edu_mediated/self_study_mediated`。

## 5. 模块详细设计【v3.0 变更】

### M1 真实数据接入（不变）

（同 v2.0 §5 M1。）

### M2 虚拟学生建模【v5.0 重构为 LLM 深度参与的四层管线】

v4.0 的 `PersonaService` 是伪随机流水线；v5.0 重构为 **LLM 深度参与的四层管线**：

```python
# src/persona_service/
class PersonaService:
    def generate_students(self, n: int, config: PersonaConfig,
                          calibrated_posterior: dict) -> list[Student]:
        """四层管线（LLM 深度参与，唯一性保证）"""
        guarantor = UniquenessGuarantor()
        students = []
        for _ in range(n):
            # L1 参数骨架：随机采样（保真锚点）
            skeleton = self._sample_skeleton(calibrated_posterior)
            # L2 身份种子：LLM 创造性生成（唯一性核心，温度 0.9–1.1）
            seed = self._llm_generate_identity_seed(skeleton, config)
            # L3 派生扩展：规则+LLM 协同（数值规则派生，叙事 LLM 续写，温度 0.7）
            persona = self._derive_and_narrate(skeleton, seed, config)
            # L4 指纹校验：唯一性保证（碰撞重采/相似改写）
            persona = guarantor.ensure_unique(persona, generator=lambda: self._regenerate(skeleton, config))
            students.append(persona)
        return students

    def generate_teachers(self, n, config, classes):
        """同四层管线；L2 身份种子含"教学风格签名"（LLM 生成）"""
    def generate_parents(self, students, config):
        """同四层管线；L2 身份种子含"教养风格签名"（LLM 生成）"""
    def generate_institutions(self, n, config):
        """【v5.0 新增】机构画像（学校/班级/课外班），LLM 生成特色签名"""
```

**LLM 调用模式（§8.7）**：分层调用（LLM 只生成 L2+L6 约 20% 字段）+ 批处理（一次调用生成 5–10 个身份种子）+ 温度分层。

**输出**：写入 `students` / `teachers` / `parents` / `institutions` 表；每个对象含 `archive_json`、`simulation_vector`、`sensitive_json`、`identity_fingerprint`、`llm_generated_fields`（LLM 参与度溯源）。

### M3 校准（不变，扩展画像分布校准）

（同 v2.0 §5 M3；新增 §4.4 的画像分布校准目标。）

### M4 干预模拟【v3.0 重构为 L-Model 驱动】

v2.0 的"学习序列 step"改为 L-Model 事件流：

```python
# src/simulate/l_model_simulator.py
def simulate_intervention_l_model(student, teacher, parent, intervention,
                                  days: int = 30) -> VirtualTrajectory:
    """L-Model 连续模拟：30 天 × 1440min 事件流"""
    trajectory = []
    for day in range(days):
        timeline = LifeTimeEngine().simulate_day(student, teacher, parent,
                                                 [intervention], date=day)
        trajectory.extend(timeline.events)
        # 疲劳/遗忘跨天连续演化
    return VirtualTrajectory(events=trajectory)
```

**干预传递**：按 §4.7 路由。**场景效应比较**：同一 intervention 分别在 target_scene=school/shadow_edu/home/self_study 下跑，产出 4 个虚拟效应量。

### M5 差距量化（不变，扩展三维失真地图）

（同 v2.0 §5 M5；失真地图改为"干预 × 场景 × 子群"三维。）

### M6 预筛排序（不变）

（同 v2.0 §5 M6。）

### M7 报告卡（不变）

（同 v2.0 §5 M7。）

### M8 《科学假设与研究计划》生成器（不变，扩展场景与角色变量）

（同 v2.0 §5 M8；Methods/Experiments 字段可含场景与角色变量。）

## 6. 数据库设计（DuckDB）【v3.0 重大变更】

```sql
-- v2.0 的 personas 表拆分为三类智能体表
CREATE TABLE students (
  student_id VARCHAR PRIMARY KEY,
  archive_json JSON,           -- 完整 20 域档案
  simulation_vector JSON,      -- 认知引擎接口（P/R 级参数）
  sensitive_json JSON,         -- S 级字段独立列（访问需角色标记）
  class_id VARCHAR,            -- 班级聚类
  assigned_teacher_id VARCHAR,
  primary_parent_id VARCHAR,
  seed INTEGER
);

CREATE TABLE teachers (
  teacher_id VARCHAR PRIMARY KEY,
  archive_json JSON,           -- T1–T8 档案
  simulation_vector JSON,      -- T-Model 参数（fidelity/style_match/经验曲线）
  sensitive_json JSON,
  seed INTEGER
);

CREATE TABLE parents (
  parent_id VARCHAR PRIMARY KEY,
  archive_json JSON,           -- P1–P6 档案
  simulation_vector JSON,      -- P-Model 参数（参与风格乘数/期望压力）
  sensitive_json JSON,
  student_id VARCHAR,          -- 关联学生
  relation_to_student VARCHAR,
  seed INTEGER
);

-- 【v5.0 新增】机构画像表
CREATE TABLE institutions (
  institution_id VARCHAR PRIMARY KEY,
  type VARCHAR,                -- school / class / shadow_edu_provider
  archive_json JSON,           -- I1–I2 机构档案（特色/氛围/风格）
  identity_fingerprint VARCHAR,
  llm_generated_fields JSON,
  seed INTEGER
);

-- 所有 persona 表增加唯一性字段（students/teachers/parents 同样 ALTER）：
-- identity_fingerprint VARCHAR, llm_generated_fields JSON

CREATE TABLE classes (
  class_id VARCHAR PRIMARY KEY,
  school_id VARCHAR,
  teacher_id VARCHAR,
  climate_json JSON,
  student_count INTEGER
);

CREATE TABLE schools (
  school_id VARCHAR PRIMARY KEY,
  profile_json JSON
);

-- L-Model 事件流存储
CREATE TABLE daily_timelines (
  timeline_id VARCHAR PRIMARY KEY,
  student_id VARCHAR,
  sim_date DATE,
  scene_events_json JSON,      -- 当日事件流（Event 列表）
  total_learning_gain DOUBLE,
  fatigue_end DOUBLE
);

-- 【v4.0 新增】社会网络与关系表
CREATE TABLE social_networks (
  network_id VARCHAR PRIMARY KEY,
  sim_date DATE,
  edges_json JSON,             -- 当日网络边列表（含类型/权重）
  metrics_json JSON            -- 密度/聚类系数/中心度分布
);

CREATE TABLE relationships (
  rel_id VARCHAR PRIMARY KEY,
  type VARCHAR,                -- friendship / romantic / conflict / teacher_student / parent_child
  party_a VARCHAR, party_b VARCHAR,
  state VARCHAR,               -- 状态机当前状态
  formed_date DATE, ended_date DATE,
  quality DOUBLE,
  history_json JSON            -- 状态转移历史
);

CREATE TABLE life_events (
  event_id VARCHAR PRIMARY KEY,
  student_id VARCHAR,
  event_type VARCHAR,          -- 事件类型库 30+ 种
  domain VARCHAR,              -- academic/family/health/social/emotional
  severity INTEGER,
  occurred_date DATE,
  impact_json JSON,            -- 对各状态变量的初始冲击
  half_life_weeks DOUBLE,
  related_rel_id VARCHAR       -- 关联关系（如恋爱事件）
);

CREATE TABLE counterfactual_runs (
  cf_id VARCHAR PRIMARY KEY,
  base_run_id VARCHAR,
  branch_run_id VARCHAR,
  modification_json JSON,      -- 分流修改内容
  comparison_json JSON         -- 轨迹对比结果
);

-- v2.0 其余表不变（real_logs/kc_params/sim_interactions/virtual_effect_sizes/
-- gap_records/llm_calls/provenance/run_states）
```

## 7. 流水线 DAG【v3.0 变更：n3 拆分 + n5 L-Model 化】

| 节点 | 模块 | 依赖 | 变更 |
|---|---|---|---|
| n1_load_realdata | M1 | — | 不变 |
| n2_fit_kc_model | M1 | n1 | 不变 |
| **n3a_gen_students** | M2 | n2 | 【新】生成学生画像 |
| **n3b_gen_teachers** | M2 | n2 | 【新】生成教师画像 |
| **n3c_gen_parents** | M2 | n3a | 【新】生成家长画像（依赖学生家庭结构） |
| n4_calibrate | M3 | n2,n3a | 扩展画像分布校准 |
| **n4b_init_network** | M4 | n3a | 【v4.0 新】初始化同伴网络（按同质性） |
| **n5_l_model_simulate** | M4 | n4,n3a,n3b,n3c,n4b | 【v4.0 重构】L-Model 2.0 多主体网络化模拟（90 天 × 5 场景 + 网络 + 事件） |
| n6_virtual_es | M4 | n5 | 不变 |
| n7_gap_analysis | M5 | n6 | 扩展三维失真地图 |
| n8_rank | M6 | n7 | 不变 |
| n9_heldout | M5 | n6 | 不变 |
| n10_report | M7+M8 | n8,n9 | 不变 |

## 8. LLM 接入与 Prompt 工程【v3.0 变更】

### 8.1 百炼平台接入（不变）

（同 v2.0 §8.1：DashScope OpenAI 兼容端点、Qwen-Plus/Max、llm_calls 表。）

### 8.2 语言智能体 Prompt【v3.0 变更：PersonaView 结构化注入】

v2.0 注入"6 参数摘要"；v3.0 注入**PrivacyGuard 过滤后的 P/R 级档案摘要**：

```
[LEARNER PROFILE - P/R LEVEL ONLY]
name={name}; grade={grade}; mbti={mbti_label}; 
interests={hobbies}; self_efficacy={0-1}; study_habits={...};
learning_style={...}; motivation_level={...}
[S-LEVEL FIELDS: NEVER INJECTED]
```

**约束不变**：is_correct 由认知引擎裁定；S 级字段物理删除（PrivacyGuard.filter_for_prompt）。

### 8.3 上下文工程（不变）

（同 v2.0 §8.3：≤2000 token，状态摘要+当前题+最近 5 次。）

### 8.4 报告撰写 Prompt（不变）

（同 v2.0 §8.4：数值/文献回验。）

### 8.5 教师/家长 LLM 调用【v3.0 新增，可选】

教师/家长的语言生成（课堂讲解、家长辅导对话）为**可选 P2 增强**；主干流程中 T-Model/P-Model 是确定性计算，不依赖 LLM。若启用，prompt 同样经 PrivacyGuard 过滤。

### 8.6 PrivacyGuard 集成【v3.0 新增】

所有 `call_qwen()` 调用前强制 `messages = PrivacyGuard.filter_for_prompt(persona)`；所有 FastAPI 响应、报告导出强制对应 filter。

### 8.7 画像生成 LLM 调用模式【v5.0 新增】

```python
# 分层调用：LLM 只生成身份种子（L2）+ 经历叙事（L6），约 20% 字段
# 温度分层：种子层高多样（0.9–1.1），叙事层保一致（0.7）
# 批处理：一次调用生成 5–10 个身份种子（JSON 数组），降调用次数 10 倍

IDENTITY_SEED_PROMPT = """
基于以下参数骨架，生成 {batch_size} 个虚拟学生的身份种子（JSON 数组）：
{skeleton_constraints}

每个身份种子包含：name（独特姓名，避免"小明/张伟"等套路名）、
birth_place（具体到区县）、family_structure_type、core_personality_seed、
unique_life_seed（一个影响学习态度的独特早期经历，要有细节，不要套路化）。

要求：
1. {batch_size} 个个体之间必须显著不同（姓名/背景/经历无重复主题）
2. 避免常见人设套路，包含至少 1 个长尾特征（如左撇子/养爬宠/父母是货车司机）
3. 输出纯 JSON 数组
"""

def generate_identity_seeds_batch(skeletons: list, batch_size: int = 8) -> list[dict]:
    """批处理生成身份种子，降低成本"""
    return call_qwen(IDENTITY_SEED_PROMPT.format(...),
                     model="qwen-plus", temperature=1.0,
                     response_format={"type": "json_object"})
```

**成本估算**（详见档案文档 §1.5）：5,000 人 ~50–80 元，20,000 人 ~200–300 元。

## 9. 后端 API 设计【v3.0 扩展】

（v2.0 §9 全部端点不变；**新增**：

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/api/runs/{run_id}/students/{student_id}` | 学生档案（P/R 级，PrivacyGuard 过滤） |
| GET | `/api/runs/{run_id}/students/{student_id}/timeline?date=` | L-Model 时间轴事件流 |
| GET | `/api/runs/{run_id}/teachers/{teacher_id}` | 教师档案（P/R 级） |
| GET | `/api/runs/{run_id}/scene_comparison` | 场景效应比较（同一干预 4 场景 g 值） |
| GET | `/api/runs/{run_id}/subgroups?dims=` | 子群切片（20+ 维度） |
| GET | `/api/runs/{run_id}/network?date=` | 社会网络快照（节点+边，按学业着色） |
| GET | `/api/runs/{run_id}/network/evolution?from=&to=` | 网络演化序列 |
| GET | `/api/runs/{run_id}/students/{id}/life_course?from=&to=` | 生命历程轨迹（状态曲线+事件标注） |
| POST | `/api/runs/{run_id}/counterfactual` | 创建反事实分支 `{modification, days}` → `cf_id` |
| GET | `/api/runs/{run_id}/counterfactual/{cf_id}/comparison` | 反事实轨迹对比 |

）

## 10. 前端设计【v3.0 扩展】

（v2.0 §10 六页不变；**新增四页**：

| 路由 | 页面 | 核心组件 |
|---|---|---|
| `/runs/:id/personas` | 画像浏览 | 学生/教师/家长列表 + 单人档案卡（P/R 级九域 Tab，S 级不显示） |
| `/runs/:id/timeline` | L-Model 时间轴 | 单学生 24h 事件流甘特图（五场景着色），干预注入点标注，疲劳曲线叠加 |
| `/runs/:id/scenes` | 场景比较 | 同一干预在学校/课外班/家庭/自学的 g 值对比柱状图 + CI |
| `/runs/:id/subgroups` | 子群切片 | 20+ 维度下拉多选，实时刷新效应量表格与失真标注 |
| `/runs/:id/network` | 社会网络可视化 | 力导向图（节点=学生按学业着色，边=友谊/恋爱/冲突分色），时间轴滑块回放演化，点击节点查看画像 |
| `/runs/:id/life_course` | 生命历程回放 | 单学生多曲线图（成绩/动机/疲劳/压力），关键事件标注（可点击详情），支持多学生叠加对比 |
| `/runs/:id/counterfactual` | 反事实对比 | 分屏显示 baseline vs modified 轨迹，差异指标汇总表，支持创建新分支表单 |

）

## 11. 配置、部署与成本【v3.0 变更】

（环境变量、运行方式不变。**成本更新**：L-Model 30 天 × 500 学生 × 事件流，LLM 调用量上升，但 FR-S4 跳过机制仍控制 ≤30%；预估 ≤150 元/次完整运行。**性能**：L-Model 事件流计算为纯 Python，可并行（学生 × 天），单机 ≤4 小时。）

## 12. 测试计划【v3.0 扩展】

（v2.0 单元/契约/集成/验收/红队不变；**新增**：画像一致性测试（T-P1–P27）、隐私隔离静态扫描（T-P10）、L-Model 时间守恒（T-P24）、场景增益方向检验（T-P27）。）

## 13. 仓库结构【v3.0 变更】

```
virtual-student-sandbox/
├── src/
│   ├── persona_service/      # 【v5.0】M2：LLM 深度参与的四层管线画像生成
│   │   ├── pipeline.py       # 四层管线主调度（L1骨架→L2种子→L3派生→L4校验）
│   │   ├── identity_seed.py  # L2：LLM 身份种子生成（批处理+温度分层）
│   │   ├── narrative.py      # L3：LLM 叙事续写（经历/兴趣/生命叙事）
│   │   ├── fingerprint.py    # L4：身份指纹 + 布隆过滤器 + 相似度检测
│   │   ├── uniqueness.py     # UniquenessGuarantor 去重补采闭环
│   │   ├── student_generator.py / teacher_generator.py / parent_generator.py / institution_generator.py
│   │   ├── coherence_engine.py
│   │   └── norms/            # 17 张常模 YAML
│   ├── agents/               # 认知引擎 + 语言智能体
│   ├── delivery/             # T-Model / P-Model / 多通道干预传递
│   ├── l_model/              # 【v4.0】L-Model 2.0 多主体引擎
│   │   ├── engine.py         # 主调度器
│   │   ├── social_network.py # 网络演化与影响传播
│   │   ├── event_engine.py   # 事件触发与半衰期
│   │   ├── relationship.py   # 关系状态机（恋爱/亲子/师生）
│   │   └── counterfactual.py # 反事实分流
│   ├── calibrate/            # M3
│   ├── simulate/             # M4（L-Model 2.0 驱动）
│   ├── privacy/              # PrivacyGuard 中间件
│   ├── gap/ rank/ report/ llm/ provenance/ api/  # 不变
├── ...
```

---

## 附录：关键配置文件模板【v3.0 新增】

**`config/persona_service.yaml`**

```yaml
student_count: 500
teacher_count: 50      # 1 教师 ↔ 10 学生（班级聚类）
parent_per_student: [1, 2]  # 1–2 家长/学生
pipeline_stages: [sample_params, derive_fields, map_labels, validate_repair, render_narrative]
coherence_max_retry: 5
norms_dir: src/persona_service/norms/
privacy_enforcement: strict  # PrivacyGuard 强制
```

**`config/l_model.yaml`**【v4.0 更新】

```yaml
simulation_days: 90          # v4.0 默认 90 天（一学期），支持 1~1095 天
time_granularity_min: 5
scenes: [school, shadow_edu, parent, self_study, fragment, social]  # 新增社交场景
time_budget_tolerance_min: 30
fatigue_recovery_sleep: 0.8
fragment_learning_discount: 0.3
# v4.0 新增
multi_agent: true
social_network:
  init_density: 0.15
  selection_effect_strength: 0.3   # 选择效应（同质性）
  influence_effect_strength: 0.4   # 影响效应（趋同）
events:
  scheduled: [exam, holiday, semester_transition]
  random_library: life_events.yaml  # 30+ 事件类型
  impact_decay_model: exponential_half_life
counterfactual:
  enabled: true
  max_branches: 5
```

**`config/intervention_delivery.yaml`**

```yaml
delivery_models: [direct, teacher_mediated, parent_mediated, shadow_edu_mediated, self_study_mediated]
target_scenes: [school, shadow_edu, home, self_study, all]
teacher_fidelity_base: 0.8
parent_style_multipliers:
  autonomy_support: 0.2
  content_explain: 0.05
  control_monitor: -0.05
  do_for_child: -0.1
shadow_edu_dose_response:
  lt_3h: 0.15
  3_to_6h: 0.05
  6_to_8h: -0.05
  gt_8h: -0.15
```

---

[^15^]: https://arxiv.org/html/2511.06078v1
[^16^]: https://ar5iv.labs.arxiv.org/html/2105.00385
[^51^]: https://arxiv.org/html/2506.19806v3
