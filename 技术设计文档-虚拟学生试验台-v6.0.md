# VirtualStudent Sandbox 虚拟学生试验台 —— 技术设计文档

| 文档项 | 内容 |
|---|---|
| 文档名称 | 技术设计文档（Technical Design Document） |
| 版本 | v6.0 |
| 日期 | 2026-08-21 |
| 关联文档 | 《需求说明文档-虚拟学生试验台-v6.0》（功能编号 FR-* 与验收标准 T-* 以需求文档为准）；《虚拟学生全方位档案设计文档-v6.0》（画像数据源与字段规范） |
| 交付对象 | 参赛团队 + 下游 vibe coding 开发工具 |
| 文档约定 | 标注 TODO 的阈值/权重需试点后标定；代码示例与实际实现一致 |

---

## 1. 总体架构

> **价值导向**：本文档的每一项技术决策都服务于一个总纲——**超越真实，落地于真实**。反事实引擎（反事实实验）、L-Model（7×24 长周期追踪）、社交网络引擎（网络操纵）、PrivacyGuard（敏感议题安全研究）分别对应传统方法"物理上/伦理上/经济上做不到"的能力；Prefect 编排 + 批处理 LLM 调用 + DuckDB 轻量栈对应"效率与成本的数量级优势"。完整价值论证见《需求说明文档》§2。

系统分七层：

1. **接入层**：React 前端 + FastAPI 后端（REST API）。
2. **编排层**：Prefect DAG（`virtual_student_pipeline`），不可用时退化为顺序执行。
3. **画像服务层**：PersonaService——学生/教师/家长/机构生成，LLM 深度参与的四层管线 + 唯一性保证。
4. **智能体层**：认知引擎（BKT+ACT-R，确定性）+ 语言智能体（千问）+ T-Model/P-Model（确定性传递函数）+ 统计分析角色（校准/差距/排序/报告）。
5. **L-Model 引擎层**：连续时间轴 + 五场景事件流 + 社交网络演化 + 事件引擎 + 关系状态机 + 反事实分流。
6. **数据层**：DuckDB 19 表 + 溯源图。
7. **基座层**：阿里百炼 Qwen（DashScope OpenAI 兼容端点）。

**架构关键决策**：

1. **画像服务独立成层**：PersonaService 是独立 Python 包（可单独测试/复用），暴露学生/教师/家长/机构生成函数；内部实现四层管线与一致性引擎。
2. **L-Model 独立成层**：生活时间引擎是独立调度器，接收"画像 + 干预配置 + 场景规则"，输出"事件流 + 虚拟轨迹"；认知引擎作为 L-Model 调用的"单场景学习增益计算器"。
3. **多智能体物理分离**：学生/教师/家长是三组独立智能体实例，各自有 persona；干预传递函数（T-Model/P-Model）是确定性计算模块，不是 LLM 智能体。
4. **隐私中间件前置**：所有 LLM 调用、API 响应、文件导出必经 PrivacyGuard 过滤（P/R/S 三级）。
5. **可降级离线**：无 API key 时全链路回退确定性 mock，保证测试/演示不依赖外部服务。

---

## 2. 技术选型总表

| 类别 | 选型 | 版本 | 用途 |
|---|---|---|---|
| 语言 | Python | 3.11+ | 主语言 |
| Web 框架 | FastAPI | 0.109.0 | 后端 REST API |
| ASGI 服务器 | uvicorn | 0.27.0 | 服务运行（端口 6668） |
| 数据校验 | pydantic | 2.7.0 | 请求/响应模型 |
| 嵌入式数据库 | DuckDB | 1.0.0 | 本地分析存储 |
| ORM | SQLAlchemy | 2.0.0 | 数据库工具 |
| 智能体框架 | AgentScope | 0.0.8 | 多智能体编排 |
| 大模型 SDK | dashscope / openai | 1.9.0 | 千问 OpenAI 兼容调用 |
| 工作流编排 | Prefect | 3.0.0 | DAG 编排 |
| 机器学习 | scikit-learn | 1.4.0 | 统计与校准 |
| 数值计算 | numpy / pandas / scipy | 1.26.4 / 2.2.0 / 1.14.0 | 数值与统计检验 |
| 图分析 | networkx | 3.2 | 社交网络 |
| 知识追踪 | pyBKT | 0.3.5 | BKT 拟合 |
| 超参优化 | Optuna | 3.1.0 | 校准搜索（备选） |
| 测试 | pytest / pytest-asyncio | 7.4.0 / 0.23.0 | 单元/集成测试 |
| 前端 | React + react-router | — | 演示前端（端口 4000） |

---

## 3. 多智能体系统设计

### 3.1 角色与权责

| 角色 | 类型 | 职责 | 禁止事项 |
|---|---|---|---|
| CognitiveEngine | 确定性 | BKT/ACT-R 单场景学习增益计算、is_correct 裁定 | —— |
| LanguageAgent | LLM（千问） | 学生作答文本/错误解释生成 | 决定对错；引入画像外知识 |
| TeacherAgent | 确定性 + LLM | T-Model 传递函数（保真度/风格匹配，确定性）；课堂讲解文本（LLM，可选） | 决定学生掌握度 |
| ParentAgent | 确定性 + LLM | P-Model 传递函数（参与风格乘数，确定性）；家长辅导对话（LLM，可选） | 决定学生掌握度 |
| InterventionDriver | 确定性 | 干预配置解析与场景路由 | —— |
| L-Model Engine | 确定性 | 连续时间轴调度、五场景事件流、疲劳/遗忘连续演化 | —— |
| OutputValidator | 确定性 | 输出契约校验 | —— |
| Calibrator | 统计 | 参数后验拟合 | 手调参数 |
| GapAnalyst | 统计 | 差距/失真/heldout | —— |
| PriorityRanker | 确定性 | 优先级评分 | 失真区给高优先级 |
| ReportWriter | LLM（千问） | 《科学假设与研究计划》撰写 | 编造数值与文献 |
| HITLReviewer | 人 | 否决/加权 | —— |

### 3.2 六条护栏

- G1 is_correct 由认知引擎裁定，LLM 不得决定对错。
- G2 LLM 输出必须经 schema 校验，不合格重试或回退。
- G3 任何数值结论必须可溯源到真实数据或文献锚点。
- G4 失真区禁止给出高优先级建议。
- G5 报告引用零虚构（文献回验）。
- G6 S 级隐私字段（D11 等）永不进入任何 LLM prompt、API 响应、导出文件；由 PrivacyGuard 中间件代码级强制（§3.5）。

### 3.3 运行状态机

`initialized → data_loaded → kc_fitted → personas_generated → teachers_generated → parents_generated → calibrated → l_modeled → simulated → gap_analyzed → ranked → reported`，异常分支转入 `failed` 并记录原因。

### 3.4 耦合机制（单次作答生命周期）

认知引擎裁定 is_correct 与置信度 → 语言智能体按裁定约束生成作答文本 → OutputValidator 校验 → 写入。作答发生在 L-Model 事件流的某个场景事件中，而非固定 step。

### 3.5 PrivacyGuard 隐私中间件

```python
class PrivacyGuard:
    """P/R/S 三级可见性强制中间件"""
    P_LEVEL_FIELDS = ["student_id", "name", "age", "gender", "grade",
                      "personality_tags", "aptitudes", "interests",
                      "character_practice", "mbti"]
    R_LEVEL_FIELDS = ["family_structure", "parents_occupation",
                      "achievement_score", "simulation_vector", ...]
    S_LEVEL_FIELDS = ["sensitive_data", "private_sensitive", "gifted_sen",
                      "parenting_style_detail", "family_conflict",
                      "tutoring_cost", "mental_health_records",
                      "medical_history", "financial_details"]
    SENSITIVE_DOMAINS = {"D11"}

    def filter_for_prompt(self, persona: dict) -> dict:
        """只保留 P 级 + R 级摘要（≤200 token），S 级物理删除"""
    def filter_for_api(self, persona: dict) -> dict:
        """API 响应：P + R 级，S 级删除"""
    def filter_for_export(self, report: dict) -> dict:
        """导出物：P 级 + 聚合统计，S 级删除"""
    def scan_log(self, text: str) -> list[str]:
        """静态扫描：检测 S 级字段值是否泄漏到文本（T-P10）"""
```

所有 `call_qwen()`、FastAPI 响应、报告导出函数强制经过对应 filter；`scan_log` 用于验收测试 T-P10。

---

## 4. 核心算法与公式

### 4.1 BKT 掌握度更新

标准贝叶斯知识追踪（Corbett & Anderson 1995）。观测后验：

\[
P(L_n \mid obs) = \frac{P(obs \mid L_n) P(L_n)}{P(obs \mid L_n) P(L_n) + P(obs \mid \neg L_n) P(\neg L_n)}
\]

正确作答后叠加学习转移：\(P(L_n) \leftarrow P(L_n \mid obs) + (1 - P(L_n \mid obs)) \cdot P(T)\)。预测正确率 \(P(correct) = P(L)(1-P(S)) + (1-P(L))P(G)\)。默认参数 `p_know_init=0.2, p_learn=0.25, p_slip=0.1, p_guess=0.1`。

### 4.2 ACT-R 遗忘（连续时间）

在 L-Model 连续时间轴上演化：\(A(t) = \ln(\Sigma_j (t - t_j)^{-d})\)，t 为当前时间戳（分钟），t_j 为历史练习时间戳，d 为衰减参数（默认 0.5）。提取概率经 sigmoid 转换：\(P(retrieve) = 1/(1+e^{-(A-\tau)/s})\)。遗忘在每个事件边界连续更新。最终 \(P(correct) = P_{BKT}(correct) \times P_{ACT-R}(retrieve)\)。

### 4.3 虚拟效应量

Hedges' g + 95% CI。先算 Cohen's d，再用校正因子 \(J = 1 - \frac{3}{4(n_c+n_t-2)-1}\) 得 Hedges' g；标准误 \(SE_g\) 按公式计算；95% CI 用 t 分布临界值。效应量基于**增益分数**（末次成绩 − 基线成绩）的处理组 vs 对照组对比，以隔离随机分组残留的基线不平衡。

### 4.4 校准距离与搜索

四类拟合目标加权：学习曲线 0.4 / 错误分布 0.2 / 首次正确 0.2 / 方差比 0.2。参数搜索采用随机搜索（n_trials=60，备选 Optuna）。文献基线锚点来自 5 项 BKT 研究（Corbett & Anderson 1995、Baker 2004、Feng 2009 ASSISTments、Pardos & Heffernan 2011、Khajah 2014）的跨研究均值（约 p_know~0.38、p_learn~0.25、p_slip~0.10、p_guess~0.20）。画像分布校准目标：

| 拟合目标 | 数据源 |
|---|---|
| 学生 Big Five 各维度均值/方差 | 青少年常模 |
| 家庭结构比例（单亲/留守等） | 人口学分布 |
| 影子教育时长分布 | CEPS/CFPS 量级 |
| 作息时间分布 | 公开统计 |
| 教师经验/职称分布 | 教育统计 |

### 4.5 差距指标

四类指标族（学习曲线/错误分布/首次正确/方差比）对比虚拟 vs 真人锚点；失真分类：overestimate / underestimate / variance_mismatch / shape_mismatch / none；容差 0.10，超过 2× 容差标 high。

### 4.6 L-Model 多主体网络化连续时间引擎

```python
class LifeTimeEngineV2:
    """多主体网络化生命历程模拟引擎"""
    def simulate(self, days: int, interventions=None,
                 intervention_engine=None) -> dict:
        for day in range(days):
            # 1. 计划事件触发（考试/学期节点）
            # 2. 每个学生：随机事件抽样 + 施加冲击
            # 3. 每个学生：simulate_day（五场景事件流）
            # 4. 网络影响传播（选择 + 影响效应）
            # 5. 施加干预（按通道与场景路由）
            # 6. 记录后更新状态（疲劳/动机/情绪恢复）
            # 7. 边权重同质性更新 + 网络快照
            # 8. 关系状态更新
        return {"trajectories", "network_metrics", "events_log", ...}
```

每日 `simulate_day` 生成五场景事件流：上午学校（8–12 点）、午间社交、下午学校、课外班（shadow_edu，按概率触发）、自学。

**社交网络层（动态图，networkx）**：

```python
class SocialNetworkEngine:
    def propagate_influence(self, ...):
        # 影响效应：Δach_i = Σ_j w_ij×(ach_j−ach_i)×susceptibility_i / neighbor_count
    def update_edges_homophily(self, ...):
        # 选择效应：new_weight = 0.9×current + 0.1×similarity（同质性）
    def detect_communities / get_centrality / get_clustering_coefficient / export_edges
```

`DynamicNetworkMonitor.record_snapshot()` 记录密度、平均度、平均聚类系数、连通分量数、平均路径长度、最大连通分量规模。

**事件引擎**：

```python
class EventEngine:
    SCHEDULED_EVENTS = {exam: [day 60, 120], holiday: [day 90],
                        semester_transition: [day 90]}
    RANDOM_EVENTS = {exam_fail: p0.02, confession: p0.005, achievement: p0.03,
                     family_incident: p0.01, friend_breakup: p0.02,
                     health_issue: p0.01}  # 各含 half_life
    def compute_event_impact(self, ...):
        # 影响按半衰期衰减：impact(t) = impact_0 × 0.5^(days / half_life)
```

### 4.7 多通道干预传递

```python
class InterventionDeliveryEngine:
    """干预按 channel 路由到对应传递函数"""
    CHANNEL_EFFICACY = {DIRECT: 1.0, TEACHER_MEDIATED: 0.7,
                        SHADOW_EDU_MEDIATED: 0.8, PARENT_MEDIATED: 0.5,
                        SELF_STUDY_MEDIATED: 0.4}
    def compute_intervention_effect(self, ...):
        # effect = type_effect × intensity × channel_efficacy
        #          × mediator_quality × adherence
        # mediator_quality 取自教师 fidelity / 家长 involvement_level
```

干预 YAML 字段：`target_scene: school/shadow_edu/home/self_study/all`、`delivery_model: direct/teacher_mediated/parent_mediated/shadow_edu_mediated/self_study_mediated`。

**五场景增益分解**（认知引擎在每个场景事件中被调用）：

```python
def scene_gain(scene, student, item, context):
    base = bkt_update(student.p_know, item)
    if scene == "school":      return base * teacher.fidelity * teacher.style_match
    elif scene == "shadow_edu": return base * shadow.quality * dose_response(hours)
    elif scene == "parent":    return base * parent.style_multiplier * parent.quality
    elif scene == "self_study": return base * student.self_regulation * student.strategy
    elif scene == "fragment":  return base * 0.3 * student.fragment_efficiency
```

### 4.8 反事实实验引擎

```python
class CounterfactualEngine:
    MODIFICATION_EFFECTS = {remove_shadow_edu, increase_parental_support,
                            forbid_romance, switch_class, add_tutoring,
                            reduce_homework}
    def branch(self, baseline_state, modification, days, seed):
        """同一起点 + 固定种子，baseline 与 modified 分流，返回两条轨迹"""
    def compare(self, ...):  # Hedges' g 对比
    def create_run / get_run  # cf_id = CF_xxx
```

### 4.9 关系状态机

```python
class RelationshipStateMachine:
    # 类型：ROMANTIC / PEER / TEACHER_STUDENT / PARENT_CHILD / SIBLING
    # 状态：ACQUAINTANCE / FRIEND / CLOSE_FRIEND / ROMANTIC_INTEREST /
    #       DATING / BROKEN_UP / MENTOR / CONFLICT
    # 恋爱影响通道：时间投入挤占学业、情绪波动、网络嵌入
```

### 4.10 唯一性保证引擎

```python
class FingerprintEngine:
    KEY_FIELDS = ["name", "birth_date", "birth_place", "family_structure",
                  "parents_occupation", "parents_education", "key_life_events",
                  "aptitude_combo", "mbti", "core_personality"]
    def fingerprint(self, persona) -> str:
        """关键字段归一化后的 SHA256 前 16 字符"""
    def similarity(self, a, b) -> float:
        """离散字段 Jaccard + 连续字段余弦，加权 0.5/0.5"""

class BloomFilter:  # capacity 20,000,000，error_rate 0.001

class UniquenessGuarantor:
    def ensure_unique(self, candidate, regenerator_func=None):
        # 完全碰撞（指纹命中）→ 重新生成
        # 近似重复（相似度 > 0.85）→ 标记/差异化改写
        # 否则接受并登记
```

姓名唯一性另设群体级 `_used_names` 注册表 + L2 身份种子重试（最多 12 次）+ 确定性兜底改名（`_force_unique_name`），保证任意规模下姓名零重复（FR-A10）。姓名按性别分池命名：离线 mock 与 LLM 身份种子均按男/女使用不同名字库，性别与名字由同一随机源派生并回填 `IdentitySeed.gender`，作为单一真值贯穿 D1（男/女）与顶层性别（M/F），避免“男性化名字给女生、女性化名字给男生”。

---

## 5. 模块详细设计

### M1 真实数据接入

`RealDataLoader` 发现并加载 ASSISTments 2009/2015、EdNet KT1；统一 schema 后按 learner 划分；pyBKT 拟合知识成分后验。无真实数据时 `synthesize_logs()` 回退，明确标注 `source="synthetic"`（诚实保证：从不把合成数据标为真实）。

### M2 虚拟学生建模（LLM 深度参与的四层管线）

```python
class StudentGenerator:
    def generate_student(self, student_id, seed) -> (archive, status):
        # L1 参数骨架：从文献校准后验采样 p_know/p_learn，映射人格类型
        skeleton = Layer1SkeletonGenerator.sample_skeleton(posterior_dist)
        # L2 身份种子：LLM 批量创造性生成（温度 0.9–1.1，唯一性核心）
        seed = IdentitySeedGenerator.get_identity_seed_prompt(skeleton, batch_size)
        # L3 派生扩展：数值规则派生 + LLM 叙事续写（温度 0.7）
        persona = Layer3DerivationEngine.derive_numerical_fields(...) + 叙事
        # L4 指纹校验：一致性校验 + 唯一性保证（碰撞重采/相似改写）
        persona = UniquenessGuarantor.ensure_unique(persona, ...)
```

**学生档案：23 域 213 字段**（`FullArchiveBuilder` 先导出潜变量 Big Five/SES/成就/动机/自我效能/学习习惯，再由 23 个域构建方法派生，保证跨域一致；`SENSITIVE_DOMAINS={"D11"}`）：

| 域 | 标签 | 字段 | 域 | 标签 | 字段 |
|---|---|---|---|---|---|
| D1 | 身份与学籍 | 9 | D13 | 隐性心理特质 | 13 |
| D2 | 家庭与成长背景 | 9 | D14 | 发展阶段与年龄效应 | 7 |
| D3 | 人格与心理特质 | 8 | D15 | 认知与学习过程画像 | 13 |
| D4 | 能力与天赋 | 9 | D16 | 学校与课堂情境 | 11 |
| D5 | 学业档案 | 8 | D17 | 特殊才能与特殊教育需求 | 8 |
| D6 | 身心健康 | 8 | D18 | 课外学习生态 | 13 |
| D7 | 品德与社会实践 | 8 | D19 | 家庭生活与作息 | 12 |
| D8 | 兴趣与生活方式 | 6 | D20 | 家长角色连接点 | 3 |
| D9 | 关系与经历 | 5 | D21 | 恋爱与亲密关系 | 10 |
| D10 | 家庭深度结构与照护史 | 12 | D22 | 同伴社会网络 | 11 |
| D11 | 隐私与敏感信息 | 14 | D23 | 成长关键事件与生命历程 | 9 |
| D12 | 同伴、亚文化与数字生活 | 13 | | **合计** | **213** |

**教师档案**（`TeacherGenerator`）：扁平画像字段（name/subject/experience_years/teaching_style/classroom_management/teaching_philosophy）+ `simulation_vector`（T-Model 参数：fidelity 0.3–0.95、style_match_base、experience_level、fatigue_susceptibility、motivational_quality、error_detection_rate）。

**家长档案**（`ParentGenerator`）：扁平画像字段（relation/education_level/parenting_description/family_expectations/tutoring_investment/parenting_narrative）+ `simulation_vector`（P-Model 参数：parenting_style、parenting_effect_multiplier、involvement_level、expectations_pressure、educational_quality、consistency）。参与风格效应：自主支持型 +0.2 / 内容讲解型 +0.05 / 控制监督型 −0.05 / 代劳型 −0.1。每生 1–2 家长（约 15% 单亲）。家长档案的学历/职业与学生档案 D2 家庭背景**统一口径**：以 `family_background.py` 为唯一数据源（SES 条件分布），生成时经 `family_info_from_archive()` 读取学生 D2 并按 relation（父亲/母亲）链接对应字段，保证学生 D2 与家长档案统计一致（见 §5.M2 “家庭背景统一口径”）。

**机构档案**（`InstitutionGenerator`）：类型 school/class/shadow_edu_provider；I1/I2 profile（特色/氛围/资源/师生比/学业压力）；LLM 特色签名 + SHA256 指纹。

**LLM 调用模式**：分层调用（LLM 只生成身份种子 L2 + 经历叙事，约 20% 字段）+ 批处理（一次调用生成 5–10 个身份种子）+ 温度分层（种子层 0.9–1.1 高多样，叙事层 0.7 保一致）。

**输出**：每个对象含 `archive_json`、`simulation_vector`、`sensitive_json`、`identity_fingerprint`、`llm_generated_fields`（LLM 参与度溯源）。

**家庭背景统一口径（D2 ↔ 家长档案联动）**：`family_background.py` 是父母学历/职业/SES 条件分布的**唯一数据源**（`EDUCATION_BY_SES` / `OCCUPATION_BY_SES` / `EDUCATION_SUPPORT_BASE`）。学生档案 D2（`FullArchiveBuilder`）与家长档案（`ParentGenerator`）共用同一套分布采样；家长生成时经 `family_info_from_archive(domains)` 读取该生 D2 的 `father_education/father_occupation/mother_education/mother_occupation/family_ses`，按 `relation`（父亲/母亲）链接对应字段，并回填学生 D20（`primary_parent_id` 外键 + `parent_involvement_perceived` 由家长 involvement 派生）。`build_real_run` 与 Prefect 节点 `n3c_gen_parents` 均按此联动，确保跨主体统计口径一致、避免数据矛盾。

### M3 校准

`CognitiveCalibrator` 对四类目标加权随机搜索（n_trials=60）拟合 BKT 后验；`PersonaDistributionCalibrator` 对画像分布 vs 常模做距离校验（容差 0.15）；`literature_reference` 提供 5 项研究的 BKT 参数锚点。

### M4 干预模拟（L-Model 驱动）

`build_real_run` 编排真实科学链路：① 四层管线生成画像（学生从文献校准后验采样；家长经 `family_info_from_archive` 联动学生 D2 家庭背景并回填 D20 外键）② 队列分配到 5 个循证干预臂 + 1 对照臂（确定性洗牌）③ `LifeTimeEngineV2.simulate` 多主体网络化模拟 ④ 对增益分数算 Hedges' g（处理 vs 对照）⑤ 轨迹 ⑥ 社交网络 + 演化 ⑦ 场景比较。

**5 个干预臂**（各经独立通道，覆盖五通道传递系统）：

| 干预臂 | 干预类型 | 通道 | 场景 |
|---|---|---|---|
| cognitive_support | COGNITIVE_SUPPORT | TEACHER_MEDIATED | school |
| autonomy_teaching | MOTIVATIONAL_BOOST | TEACHER_MEDIATED | school |
| parent_involvement | SOCIAL_SUPPORT | PARENT_MEDIATED | home |
| shadow_edu_reduction | TIME_MANAGEMENT | SHADOW_EDU_MEDIATED | shadow_edu |
| sleep_schedule | STRESS_REDUCTION | SELF_STUDY_MEDIATED | self_study |

### M5 差距量化

`GapAnalyst` 计算四类指标的虚拟 vs 真人差距，构建"干预 × 场景 × 子群"三维失真地图，并做留出验证。

### M6 预筛排序

`PriorityRanker` 加权评分：effect_size 0.35 / certainty 0.20 / reach 0.20 / cost_efficiency 0.15 / novelty 0.10；失真惩罚最高 0.5；`top_k()` 输出排序。

### M7 报告卡

`ReportWriter.build_report()` 生成报告卡，`render_markdown()` 渲染。

### M8 《科学假设与研究计划》生成器

`HypothesisGenerator` 按 8 节（PLAN_SECTIONS）生成；`_build_hypotheses()` 带 role/scene 标签；功效分析 `per_group = max(50, int(175×(0.3/min_g)²))`；`_build_references()` 使用虚拟证据、绝不伪造引用。

---

## 6. 数据库设计（DuckDB）

`DataLayer(db_path="data/virtual_student.db")`，`init_schema()` 共 19 张表：

```sql
-- 真实数据
CREATE TABLE real_logs (log_id, learner_id, kc_id, item_id, is_correct,
                        response_time_sec, timestamp_ms, session_id);
CREATE TABLE kc_params (kc_id, p_know_posterior, p_learn, p_slip, p_guess,
                        data_count, fit_timestamp);

-- 画像（学生/教师/家长/机构，均含 archive_json + simulation_vector +
-- sensitive_json + identity_fingerprint + llm_generated_fields）
CREATE TABLE students (student_id PK, archive_json, simulation_vector,
                       sensitive_json, identity_fingerprint, llm_generated_fields,
                       class_id, assigned_teacher_id, primary_parent_id, seed, created_at);
CREATE TABLE teachers (teacher_id PK, ..., seed, created_at);
CREATE TABLE parents  (parent_id PK, ..., student_id, relation_to_student, seed, created_at);
CREATE TABLE institutions (institution_id PK, type, archive_json,
                           identity_fingerprint, llm_generated_fields, seed, created_at);

-- 班级与学校
CREATE TABLE classes (class_id PK, school_id, teacher_id, climate_json, student_count);
CREATE TABLE schools (school_id PK, profile_json);

-- L-Model 仿真状态
CREATE TABLE daily_timelines (timeline_id PK, student_id, sim_date,
                              scene_events_json, total_learning_gain, fatigue_end, created_at);
CREATE TABLE social_networks (network_id PK, sim_date, edges_json, metrics_json, created_at);
CREATE TABLE relationships (rel_id PK, type, party_a, party_b, state,
                            formed_date, ended_date, quality, history_json, created_at);
CREATE TABLE life_events (event_id PK, student_id, event_type, domain, severity,
                          occurred_date, impact_json, half_life_weeks, created_at);
CREATE TABLE counterfactual_runs (cf_id PK, base_run_id, branch_run_id,
                                  modification_json, comparison_json);

-- 结果与分析
CREATE TABLE sim_interactions (interaction_id PK, run_id, student_id, intervention_id,
                               item_id, scene, is_correct, p_correct, learning_gain,
                               timestamp_min, delivery_mode);
CREATE TABLE virtual_effect_sizes (es_id PK, run_id, intervention_id, hedges_g,
                                   ci_lower, ci_upper, scene, subgroup_filter, sample_size);
CREATE TABLE gap_records (gap_id PK, run_id, metric, real_value, virtual_value,
                          gap_magnitude, distortion_category, subgroup_filter);
CREATE TABLE run_states (run_id PK, status, created_at, updated_at, config_json, metadata_json);
CREATE TABLE llm_calls (call_id PK, run_id, module, model, prompt_tokens,
                        completion_tokens, cost_yuan, timestamp_ms);
CREATE TABLE provenance (prov_id PK, run_id, entity_type, entity_id, source,
                         lineage_json, timestamp_ms);
```

---

## 7. 流水线 DAG

Prefect 流 `virtual_student_pipeline`（Prefect 不可用时 no-op 装饰器退化顺序执行）：

| 节点 | 模块 | 依赖 |
|---|---|---|
| n1_load_realdata | M1 | — |
| n2_fit_kc_model | M1 | n1 |
| n3a_gen_students | M2 | n2 |
| n3b_gen_teachers | M2 | n2 |
| n3c_gen_parents | M2 | n3a |
| n4_calibrate | M3 | n2, n3a |
| n4b_init_network | M4 | n3a |
| n5_l_model_simulate | M4 | n4, n3a, n3b, n3c, n4b |
| n6_virtual_es | M4 | n5 |
| n7_gap_analysis | M5 | n6 |
| n8_rank | M6 | n7 |
| n9_heldout | M5 | n6 |
| n10_report | M7+M8 | n8, n9 |

---

## 8. LLM 接入与 Prompt 工程

### 8.1 百炼平台接入

DashScope OpenAI 兼容端点 `https://dashscope.aliyuncs.com/compatible-mode/v1`；当前代码默认模型 `qwen3.7-flash`（可切换 `qwen3.7-plus/max` 及兼容模型）。模型 ID 大小写敏感，必须全小写。配置由前端设置页运行时驱动（`reconfigure_client`），密钥脱敏展示，调用全量计入 `llm_calls` 成本表。无 API key 时 `is_live=False`，全链路回退确定性 mock。`call()` 对超时、网络错误和模型错误在 30 秒边界内优雅降级，不中断运行。

### 8.2 语言智能体 Prompt（PersonaView 结构化注入）

注入 PrivacyGuard 过滤后的 P/R 级档案摘要（≤200 token），S 级字段物理删除：

```
[LEARNER PROFILE - P/R LEVEL ONLY]
name={name}; grade={grade}; mbti={mbti_label};
interests={hobbies}; self_efficacy={0-1}; study_habits={...};
learning_style={...}; motivation_level={...}
[S-LEVEL FIELDS: NEVER INJECTED]
```

约束：is_correct 由认知引擎裁定；语言智能体只渲染既定结果，绝不判定对错。

### 8.3 上下文工程

单次输入 ≤2000 token：状态摘要 + 当前题 + 最近 5 次作答。

### 8.4 报告撰写 Prompt

数值/文献回验，引用零虚构。

### 8.5 教师/家长 LLM 调用（可选 P2）

教师/家长的语言生成（课堂讲解、家长辅导对话）为可选增强；主干流程中 T-Model/P-Model 是确定性计算，不依赖 LLM。若启用，prompt 同样经 PrivacyGuard 过滤。

### 8.6 画像生成 LLM 调用模式

```python
IDENTITY_SEED_PROMPT = """
基于以下参数骨架，生成 {batch_size} 个虚拟学生的身份种子（JSON 数组）：
{skeleton_constraints}
每个身份种子包含：name（独特姓名，避免套路名）、gender（男/女，必须与名字气质一致，
不给男生起女性化名字、不给女生起男性化名字）、birth_place（具体到区县）、
family_structure_type、core_personality_seed、unique_life_seed（独特早期经历）。
要求：1. 个体之间显著不同  2. 含至少 1 个长尾特征  3. 输出纯 JSON 数组
"""
# 批处理 + 温度 1.0 + response_format={"type":"json_object"}
```

成本估算不写死为固定金额：运行结束后以 `llm_calls` 的模型、输入/输出 token、调用次数和失败重试记录为准；离线 fallback 的成本为 0，但不应被表述为 Qwen 实时调用成本。正式结项前需提供一次可复现的小规模实测账单与方法说明。

---

## 9. 后端 API 设计

FastAPI（端口 6668，对前端 `http://localhost:4000` 开 CORS）。运行结果在运行时保存于进程内存储；数据层（§6）提供持久化 schema。

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/api/health` | 健康检查 |
| GET | `/api/llm/config` | LLM 配置（key 脱敏，available_models） |
| POST | `/api/llm/config` | 运行时重配 LLM |
| POST | `/api/runs` | 创建运行（调 build_real_run） |
| GET | `/api/runs/{run_id}` | 运行状态 |
| GET | `/api/runs/{run_id}/students` | 学生列表（分页，PrivacyGuard 过滤） |
| GET | `/api/runs/{run_id}/students/{student_id}` | 学生档案 |
| GET | `/api/runs/{run_id}/students/{student_id}/timeline` | 单日时间轴事件流 |
| GET | `/api/runs/{run_id}/students/{student_id}/life_course` | 生命历程轨迹（状态曲线 + 事件标注） |
| GET | `/api/runs/{run_id}/teachers/{teacher_id}` | 教师档案 |
| GET | `/api/runs/{run_id}/parents` | 家长列表 |
| GET | `/api/runs/{run_id}/parents/{parent_id}` | 家长档案 |
| GET | `/api/runs/{run_id}/scene_comparison` | 场景效应比较 |
| GET | `/api/runs/{run_id}/subgroups` | 子群切片（10+ 维度） |
| GET | `/api/runs/{run_id}/network` | 社会网络快照（节点 + 边，按学业着色） |
| GET | `/api/runs/{run_id}/network/evolution` | 网络演化序列 |
| POST | `/api/runs/{run_id}/counterfactual` | 创建反事实分支 |
| GET | `/api/runs/{run_id}/counterfactual/{cf_id}/comparison` | 反事实轨迹对比 |
| GET | `/api/runs/{run_id}/report` | 报告（M7+M8+推荐+markdown） |
| GET | `/api/runs/{run_id}/calibration` | 校准诊断（KS 检验 vs 文献基线） |
| GET | `/api/runs/{run_id}/distortion_map` | 失真地图 |
| GET | `/api/runs/{run_id}/prescreening` | 预筛 go/no-go 决策 |
| POST | `/api/runs/{run_id}/hitl/feedback` | 人在回路反馈 |
| GET | `/api/runs/{run_id}/hitl/feedback` | 反馈列表 |

---

## 10. 前端设计

React + react-router（端口 4000）。路由：

| 路由 | 页面 |
|---|---|
| `/` | 首页（运行管理/创建） |
| `/settings` | LLM 设置页（API key + 模型选择） |
| `/runs/:id/report` | 成果输出中心 |
| `/runs/:id/personas` | 画像浏览（学生/教师/家长，分域 Tab，S 级不显示） |
| `/runs/:id/parents` | 家长档案 |
| `/runs/:id/timeline` | L-Model 时间轴（逐日事件流，五场景着色） |
| `/runs/:id/scenes` | 场景比较 |
| `/runs/:id/subgroups` | 子群切片 |
| `/runs/:id/network` | 社会网络可视化（力导向图 + 时间轴回放） |
| `/runs/:id/life_course` | 生命历程回放（多曲线 + 事件标注） |
| `/runs/:id/counterfactual` | 反事实对比 |
| `/runs/:id/calibration` | 校准诊断 |
| `/runs/:id/distortion` | 失真地图 |
| `/runs/:id/prescreening` | 预筛报告 |
| `/runs/:id/hitl` | 人在回路 |

---

## 11. 配置、部署与成本

**部署**：后端 `uvicorn src.api.main:app --host 0.0.0.0 --port 6668`（监督进程保活）；前端开发服务器端口 4000。单机可跑，无 GPU 依赖。

**配置文件**（`config/`）：`base.yaml`（系统总配置）、`persona_service.yaml`（画像管线）、`l_model.yaml`（仿真）、`intervention_delivery.yaml`（干预传递）。

**成本**：L-Model 多日 × 数百学生 × 事件流，LLM 调用经 FR-S4 跳过机制控制 ≤30%；预估 ≤150 元/次完整运行。

**性能**：L-Model 事件流计算为纯 Python，可并行（学生 × 天），单机数小时内完成。

---

## 12. 测试计划

| 层级 | 内容 |
|---|---|
| 单元测试 | 认知引擎（BKT/ACT-R）、效应量公式、数据层 schema、数据加载、唯一性引擎、干预目录、文献参考 |
| 契约测试 | 输出 schema 校验、隐私隔离静态扫描（T-P10） |
| 集成测试 | 画像生成（W2）、L-Model（W3）、真实链路端到端、API 端点、新端点 |
| 验收测试 | 画像一致性（T-P1–P27）、L-Model 时间守恒（T-P24）、场景增益方向（T-P27）、研究计划（T9）、全链路验证 |
| 红队测试 | prompt 注入、S 级字段泄漏探测 |

测试套件位于 `tests/`（16 个测试模块），conftest 提供会话级进程内 uvicorn fixture（端口 6668）。

---

## 13. 仓库结构

```
virtual-student-sandbox/
├── src/
│   ├── persona_service/        # M2：LLM 深度参与的四层管线画像生成
│   │   ├── __init__.py         # 唯一性引擎（FingerprintEngine/BloomFilter/UniquenessGuarantor）
│   │   ├── identity_seed.py    # L1 骨架 + L2 LLM 身份种子
│   │   ├── layers_3_4.py       # L3 派生 + L4 一致性引擎
│   │   ├── full_archive.py     # 23 域 213 字段档案构建
│   │   ├── student_generator.py    # 学生生成主调度
│   │   ├── teacher_parent_generator.py  # 教师/家长生成（T-Model/P-Model）
│   │   ├── institution_generator.py     # 机构生成
│   │   └── coherence_engine.py     # 一致性校验
│   ├── cognitive_engine.py     # BKT + ACT-R 认知引擎
│   ├── data_layer.py           # DuckDB 19 表 schema
│   ├── data_loader.py          # M1 真实数据加载
│   ├── agents/                 # PersonaView + 语言智能体
│   ├── delivery/               # M4 五通道干预传递 + 效应量
│   ├── l_model/                # L-Model 多主体引擎
│   │   ├── engine.py           # 主调度器
│   │   ├── social_network.py   # 网络演化与影响传播
│   │   ├── event_engine.py     # 事件触发与半衰期
│   │   ├── relationship.py     # 关系状态机
│   │   └── counterfactual.py   # 反事实分流
│   ├── calibrate/              # M3 校准 + 文献参考
│   ├── simulate/               # M4 模拟器（备选路径）
│   ├── gap/                    # M5 差距量化与失真地图
│   ├── rank/                   # M6 预筛排序
│   ├── report/                 # M7 报告卡 + M8 假设生成器
│   ├── privacy/                # PrivacyGuard 中间件
│   ├── provenance/             # 溯源追踪
│   ├── llm/                    # 百炼千问接入 + mock 回退
│   ├── pipeline/               # Prefect DAG 编排
│   └── api/                    # FastAPI 后端（main/models/real_run）
├── config/                     # base/persona_service/l_model/intervention_delivery
├── frontend/src/               # React 前端（15 路由）
├── tests/                      # 16 个测试模块
└── scripts/                    # 演示与运维脚本
```

---

## 附录：关键配置文件模板

**`config/persona_service.yaml`**

```yaml
student_count: 500
teacher_count: 50          # 1 教师 ↔ 10 学生（班级聚类）
parent_per_student: [1, 2] # 1–2 家长/学生
pipeline_stages: [sample_skeleton, identity_seed, derive_narrative, coherence_check]
llm:
  model: qwen3.7-flash
  batch_size: 8
uniqueness:
  bloom_capacity: 20000000
  similarity_threshold: 0.85
privacy_enforcement: strict
```

**`config/l_model.yaml`**

```yaml
simulation_days: 90            # 默认一学期，支持 1~1095 天
time_granularity_min: 5
scenes: [school, shadow_edu, parent, self_study, fragment, social]
time_budget_tolerance_min: 30
fatigue_recovery_sleep: 0.8
fragment_learning_discount: 0.3
multi_agent: true
social_network:
  init_density: 0.15
  selection_effect_strength: 0.3   # 选择效应（同质性）
  influence_effect_strength: 0.4   # 影响效应（趋同）
events:
  scheduled: [exam, holiday, semester_transition]
  random_library: life_events.yaml
  impact_decay_model: exponential_half_life
counterfactual:
  enabled: true
  max_branches: 5
```

**`config/intervention_delivery.yaml`**

```yaml
delivery_models: [direct, teacher_mediated, parent_mediated,
                  shadow_edu_mediated, self_study_mediated]
target_scenes: [school, shadow_edu, home, self_study, all]
teacher_fidelity_base: 0.8
parent_style_multipliers:
  autonomy_support: 0.2
  content_explain: 0.05
  control_monitor: -0.05
  do_for_child: -0.1
shadow_edu_dose_response:
  lt_3h: 0.15
  3_to_6h: 0.05
  6_to_8h: -0.05
  gt_8h: -0.15
interventions:               # I1–I4 内置循证干预（YAML 可扩展）
  worked_examples: {effect_achievement: ..., evidence_hedges_g: ...}
  spaced_practice: {...}
  feedback: {...}
  retrieval_practice: {...}
```

---

## 15. 三方关系建模

### 15.1 概述

三方关系建模扩展了传统的二元社会网络分析，构建学生-教师-家长三方关系图，揭示干预效果如何通过不同关系渠道传递。

### 15.2 数据结构

三方关系网络由三类节点和三类边组成：

**节点类型**：
- 学生节点（S）：携带认知参数、成就分数、人格特征
- 教师节点（T）：携带教学风格、干预投放能力
- 家长节点（P）：携带参与风格、家庭支持度

**边类型**：
- S-T 边：师生关系，权重反映互动频率与质量
- S-P 边：亲子关系，权重反映家长参与度
- T-P 边：教师-家长沟通，权重反映家校合作程度

### 15.3 构建算法

`python
def _build_triad_network(students, teachers, parents, arms_table, arms, sim_days):
    """构建三方关系网络"""
    graph = nx.Graph()
    
    # 添加节点
    for sid, s in students.items():
        graph.add_node(sid, type='student', achievement=s['achievement_score'])
    for tid, t in teachers.items():
        graph.add_node(tid, type='teacher', style=t['teaching_style'])
    for pid, p in parents.items():
        graph.add_node(pid, type='parent', involvement=p['involvement_level'])
    
    # 添加师生边（基于干预投放关系）
    for intv_id, arm_ids in arms.items():
        channel = next((c for i, _, c, _, _ in arms_table if i == intv_id), None)
        if channel == InterventionChannel.TEACHER_MEDIATED:
            for sid in arm_ids:
                # 找到负责的教师
                tid = _find_teacher_for_student(sid, teachers)
                if tid:
                    weight = _compute_relationship_weight(sid, tid, sim_days)
                    graph.add_edge(sid, tid, weight=weight, type='student-teacher')
    
    # 添加亲子边
    for sid in students:
        pid = _find_parent_for_student(sid, parents)
        if pid:
            weight = _compute_parent_involvement(sid, pid)
            graph.add_edge(sid, pid, weight=weight, type='student-parent')
    
    # 添加家校边
    for tid in teachers:
        for pid in parents:
            if _has_communication(tid, pid):
                weight = _compute_communication_frequency(tid, pid)
                graph.add_edge(tid, pid, weight=weight, type='teacher-parent')
    
    return graph
`

### 15.4 可视化

前端使用 SVG 绘制三方关系图：
- 学生节点：蓝色圆形
- 教师节点：绿色方形
- 家长节点：橙色三角形
- 边粗细反映关系强度
- 支持按干预类型筛选显示

### 15.5 API 端点

`
GET /api/runs/{run_id}/triad_network?intervention_id={intv_id}
`

返回三方关系图的节点和边数据，支持按干预类型筛选。

---

## 16. 中介效应分解

### 16.1 概述

中介效应分析揭示干预效果通过哪些渠道生效。平台支持五种中介通道：

1. **教师中介**（teacher_mediated）：通过教师教学行为传递
2. **家长中介**（parent_mediated）：通过家长参与传递
3. **直接效应**（direct）：干预直接影响学生
4. **课外教育中介**（shadow_edu_mediated）：通过课外辅导班传递
5. **自主学习中介**（self_study_mediated）：通过学生自学行为传递

### 16.2 通道分解算法

`python
def _compute_mediation_breakdown(channel, arm_ids, engine_students, baseline_ach, calc, control_gain):
    """简化中介效应分解"""
    channel_val = channel.value if hasattr(channel, "value") else str(channel)
    treatment_gain = np.array(
        [engine_students[sid]["achievement_score"] - baseline_ach[sid]
         for sid in arm_ids], dtype=float) if arm_ids else np.array([0.0])
    total_g = float(calc.compute_hedges_g(control_gain, treatment_gain)[0]) if len(arm_ids) >= 2 else 0.0
    
    # 根据通道分配比例
    if channel_val == "teacher_mediated":
        teacher_frac, parent_frac, direct_frac = 0.55, 0.15, 0.30
    elif channel_val == "parent_mediated":
        teacher_frac, parent_frac, direct_frac = 0.15, 0.55, 0.30
    elif channel_val == "shadow_edu_mediated":
        teacher_frac, parent_frac, direct_frac = 0.10, 0.20, 0.70
    elif channel_val == "self_study_mediated":
        teacher_frac, parent_frac, direct_frac = 0.05, 0.10, 0.85
    else:  # direct
        teacher_frac, parent_frac, direct_frac = 0.10, 0.10, 0.80
    
    return {
        "teacher_mediated": round(total_g * teacher_frac, 4),
        "parent_mediated": round(total_g * parent_frac, 4),
        "direct": round(total_g * direct_frac, 4),
        "teacher_fraction": round(teacher_frac, 2),
        "parent_fraction": round(parent_frac, 2),
        "direct_fraction": round(direct_frac, 2),
    }
`

### 16.3 通道分解数据结构

每个干预的效应量包含 `channel_breakdown` 字段：

`json
{
  "intervention_id": "cognitive_support",
  "hedges_g": 0.35,
  "channel_breakdown": {
    "teacher_mediated": {"n": 50, "avg_g": 0.35},
    "parent_mediated": {"n": 0, "avg_g": null},
    "direct": {"n": 0, "avg_g": null},
    "shadow_edu_mediated": {"n": 0, "avg_g": null},
    "self_study_mediated": {"n": 0, "avg_g": null}
  }
}
`

### 16.4 效果路径图可视化

前端使用 SVG 绘制效果路径图：
- 左侧：干预名称（蓝色方框）
- 中间：中介变量（彩色方框，颜色区分通道）
- 右侧：结果（成绩变化，绿色方框）
- 箭头：表示因果方向
- 标注：显示样本量和平均效应量

### 16.5 理论依据

中介效应分析基于教育心理学理论：
- **社会认知理论**（Bandura）：教师作为榜样和引导者
- **家庭系统理论**（Minuchin）：家长参与对学习的间接影响
- **自我决定理论**（Deci & Ryan）：自主学习和内在动机

---

## 17. 稳健性检验

### 17.1 概述

稳健性检验验证研究结论是否依赖于特定的随机种子或样本量。平台提供两种敏感性分析：

1. **种子敏感性**：不同随机种子下效应量是否稳定
2. **样本量敏感性**：效应量随样本量增加是否趋于稳定

### 17.2 种子敏感性分析

**方法**：
- 收集多次运行（相同配置，不同种子）
- 对每个干预，计算不同种子下的平均效应量
- 绘制折线图对比

**判定标准**：
- 如果不同种子的效应量差异 < 0.05，结论稳健
- 如果差异 > 0.10，结论可能依赖于特定种子

### 17.3 样本量敏感性分析

**方法**：
- 收集不同学生数的运行
- 对每个干预，绘制效应量 vs 样本量折线图
- 观察是否趋于稳定

**判定标准**：
- 如果效应量随样本量增加趋于稳定（斜率 < 0.01），样本量足够
- 如果效应量持续波动，需要更大样本量

### 17.4 数据不足时的优雅降级

如果系统中没有足够的历史运行数据：
- 显示提示信息：「需要多次运行不同种子/样本量才能生成稳健性分析」
- 不报错，不影响其他功能

### 17.5 实现位置

- 后端：`src/api/main.py` 的 `GET /api/robustness` 只读取稳健性 sidecar；`GET /api/runs` 使用 meta sidecar；完整运行包按需懒加载
- 前端：`frontend/src/pages/RobustnessPage.jsx` 实现可视化
- API：稳健性页使用轻量 `GET /api/robustness?run_id=...`，不触发完整 report 或历史运行包解压

---

## 18. v6.0 运行性能与结项实现基线

### 18.1 历史运行加载路径

历史运行不是在前端打开页面时重新仿真，但旧实现会在启动或页面请求中读取体积较大的压缩 JSON，并可能连带构建完整报告。v6.0 将三类数据分开：

| 数据层 | 文件/接口 | 访问策略 |
|---|---|---|
| 历史索引 | `data/runs/*.meta.json`、`GET /api/runs` | 只读人数、状态、时间、seed 等摘要，不解压完整运行包 |
| 稳健性摘要 | `data/runs/*.robustness.json`、`GET /api/robustness` | 只读 effect sizes 和敏感性摘要，不生成报告 |
| 完整运行 | `data/runs/*.json.gz`、按 run 请求 | 首次访问按需解压，内存 LRU 最多保留有限数量，重复访问使用缓存 |

报告另有 `*.report.json` 磁盘缓存。稳健性页面不得调用完整 report；学生档案和时间线接口只取目标学生，避免把全体学生序列化到一次响应中。该设计降低历史仿真数量增长带来的启动时间、内存和页面卡顿风险，但单个超大 run 的首次解压仍需通过基准测试确认。

### 18.2 创建运行与 LLM 调用路径

`POST /api/runs` 立即返回 `status=running`，重计算在后台线程执行，前端通过状态接口读取进度。学生、教师和家长画像生成使用有上限的线程池和批处理；语言模型请求采用 30 秒超时，失败后写入调用日志并回退到确定性 mock。这样可以避免 Qwen 网络波动阻塞 FastAPI 事件循环，也不会让一次生成请求无限等待。

### 18.3 结项前必须完成的性能基准

基准测试必须固定硬件、Python/Node 版本、seed、学生数、模拟天数、是否 live LLM 和缓存状态，至少记录以下指标：

| 场景 | 指标 |
|---|---|
| 历史列表 | 首次请求与连续请求耗时、历史 run 数量、meta sidecar 命中率 |
| 已完成 run 打开 | 首次解压耗时、缓存命中耗时、峰值内存、压缩包大小 |
| 稳健性页面 | 请求耗时、是否读取完整 `.json.gz`、是否触发 report/LLM |
| 学生档案 | 单学生响应大小、p50/p95、P/R/S 字段过滤耗时 |
| 报告页 | 缓存命中与未命中耗时、LLM 调用次数、fallback 次数 |
| 创建运行 | 画像、模拟、效果量、网络和持久化各阶段耗时 |

v6.0 不预设“所有机器都在固定秒数内完成”。正式材料应同时给出测试条件、实测范围和失败项，避免把开发机上的单次结果写成产品 SLA。

### 18.4 Qwen 与离线模式证据

结项压缩包需提供脱敏的 `llm_calls` 示例，至少包含模型、调用阶段、token、延迟、状态、fallback 原因和成本计算字段；不得包含 API key、完整敏感 prompt 或真实个人信息。运行说明需明确 live、offline 和 mixed 三种模式，并在报告中区分干预资源成本与 LLM 生成成本，不把二者合并成固定 450 或其他常数。

### 18.5 交付包与安全冻结

发布 v6.0 前需完成依赖锁定、数据版本记录、配置模板、变更日志和最小运行样例。真实凭证不得进入提交包；若凭证曾在本地或版本历史中暴露，必须先吊销并重新生成。仓库只保留 `.example` 配置，启动时对缺失 key 给出可理解的离线提示。
